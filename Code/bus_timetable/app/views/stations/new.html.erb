<%= content_for(:title, 'New station | Bus Timetable') %>

  <div class="row">
    <%= render 'layouts/errors', { object: @station } %>

    <h1>New Bus Station:</h1>

    <%= render 'form' %>
  </div>
